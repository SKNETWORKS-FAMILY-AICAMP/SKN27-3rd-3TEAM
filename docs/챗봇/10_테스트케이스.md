# 테스트 케이스 (Test Cases)

**프로젝트명:** 포켓몬 AI 챗봇  
**문서 버전:** v1.1  
**작성일:** 2025-05-14  
**최종 수정:** 2025-05-14 (search_pokemon_weakness 단위/통합 TC 추가, search_type_relations TC 업데이트)

---

## 1. 테스트 범위 및 전략

| 테스트 유형 | 대상 | 도구 |
|-----------|------|------|
| 단위 테스트 (Unit) | Tool 함수, ChatHistory 모듈 | pytest |
| 통합 테스트 (Integration) | Agent ↔ DB, Agent ↔ Neo4j | pytest + testcontainers |
| E2E 테스트 | 전체 대화 시나리오 | pytest + httpx |
| 보안 테스트 | SQL 인젝션 차단 | pytest |
| 성능 테스트 | 응답 시간 측정 | pytest-benchmark |

---

## 2. 단위 테스트

### 2.1 search_pokemon_db Tool

| TC-ID | 테스트명 | 입력 | 기대 결과 | 우선순위 |
|-------|---------|------|---------|---------|
| UT-DB-01 | 정상 SELECT 실행 | `SELECT name FROM pokemon LIMIT 3` | `✅ 3개 결과:` 로 시작하는 JSON | 필수 |
| UT-DB-02 | INSERT 차단 | `INSERT INTO pokemon VALUES (...)` | `오류: SELECT 문만 허용됩니다.` | 필수 |
| UT-DB-03 | UPDATE 차단 | `UPDATE pokemon SET name='test'` | `오류: UPDATE 명령은 허용되지 않습니다.` | 필수 |
| UT-DB-04 | DROP 차단 | `DROP TABLE pokemon` | `오류: DROP 명령은 허용되지 않습니다.` | 필수 |
| UT-DB-05 | 코드 블록 제거 | ` ```sql SELECT 1 ``` ` | 정상 실행 결과 반환 | 필수 |
| UT-DB-06 | 결과 없는 쿼리 | `SELECT * FROM pokemon WHERE name='없는포켓몬'` | `조회 결과가 없습니다.` | 선택 |
| UT-DB-07 | 잘못된 컬럼명 | `SELECT atk FROM pokemon` | `SQL 오류: ...힌트:...` 포함 | 필수 |
| UT-DB-08 | 복합 JOIN 쿼리 | 불꽃 타입 + 공격력 기준 정렬 쿼리 | 정상 결과 반환 | 필수 |

### 2.2 search_flavor_text Tool

| TC-ID | 테스트명 | 입력 | 기대 결과 | 우선순위 |
|-------|---------|------|---------|---------|
| UT-VEC-01 | 정상 벡터 검색 | `"귀여운 포켓몬"` | `✅ 관련 도감 설명:` 포함, 5개 이하 문서 | 필수 |
| UT-VEC-02 | 결과 없음 처리 | 매우 비관련 쿼리 | `검색 결과가 없습니다.` | 선택 |
| UT-VEC-03 | MMR 다양성 확인 | `"불꽃 포켓몬"` | 동일 포켓몬 반복 없이 다양한 결과 | 필수 |
| UT-VEC-04 | 상위 5개 제한 확인 | 임의 쿼리 | 반환 문서 수 ≤ 5 | 필수 |

### 2.3 search_evolution_chain Tool

| TC-ID | 테스트명 | 입력 | 기대 결과 | 우선순위 |
|-------|---------|------|---------|---------|
| UT-EVO-01 | 중간 진화형 탐색 | `"리자드"` | 파이리(전) + 리자몽(후) 모두 포함 | 필수 |
| UT-EVO-02 | 최종 진화형 탐색 | `"리자몽"` | 파이리, 리자드 진화 전 표시 | 필수 |
| UT-EVO-03 | 미진화 포켓몬 | `"파이리"` | 진화 후만 표시 | 필수 |
| UT-EVO-04 | 없는 포켓몬명 | `"없는포켓몬"` | `진화 정보를 찾을 수 없습니다.` | 필수 |
| UT-EVO-05 | 아이템 진화 | `"이브이"` (특정 진화형) | 아이템 이름 포함 | 선택 |
| UT-EVO-06 | 부분 이름 매칭 | `"피카"` | 피카츄 결과 반환 | 선택 |
| UT-EVO-07 | 중복 경로 방지 (LIMIT 1 + DISTINCT) | 여러 폼이 있는 포켓몬명 | 중복 진화 경로 없이 단일 결과 | 필수 |

### 2.4 search_type_relations Tool

| TC-ID | 테스트명 | 입력 | 기대 결과 | 우선순위 |
|-------|---------|------|---------|---------|
| UT-TYPE-01 | 불꽃 타입 공격 효과 | `"불꽃"` | 풀, 얼음, 벌레, 강철에 효과적 포함 | 필수 |
| UT-TYPE-02 | 없는 타입 | `"우주"` | `타입 정보를 찾을 수 없습니다.` | 필수 |
| UT-TYPE-03 | 무효(0배) 섹션 표시 | `"고스트"` | `🚫 공격 시 무효 (0배):` 섹션 포함 | 필수 |
| UT-TYPE-04 | 역방향 약점 | `"물"` | 전기, 풀에 약점 표시 | 필수 |
| UT-TYPE-05 | 약점 포켓몬 예시 | `"불꽃"` | `📋 약점 포켓몬 예시:` 섹션 포함, ≤5개 | 선택 |
| UT-TYPE-06 | 면역 포켓몬 예시 | `"노말"` | `🛡️ 면역 포켓몬 예시:` 섹션 포함 | 선택 |
| UT-TYPE-07 | 단일 타입 필터 정확도 | `"불꽃"` | 듀얼 타입 포켓몬 영향 없이 순수 배율 반환 | 필수 |

### 2.5 search_pokemon_weakness Tool ✨ NEW

| TC-ID | 테스트명 | 입력 | 기대 결과 | 우선순위 |
|-------|---------|------|---------|---------|
| UT-WEAK-01 | 단일 타입 약점 조회 | `"피카츄"` | 땅 타입 2배 약점 포함 | 필수 |
| UT-WEAK-02 | 듀얼 타입 4배 약점 | `"리자몽"` | `💀 4배 약점: 바위` 포함 | 필수 |
| UT-WEAK-03 | 듀얼 타입 0.25배 저항 | 해당 포켓몬 | `🛡️ 0.25배 저항:` 섹션 포함 | 필수 |
| UT-WEAK-04 | 면역 표시 | `"리자몽"` | `✨ 면역: 땅` 포함 | 필수 |
| UT-WEAK-05 | 보유 타입 표시 | `"이상해씨"` | `보유 타입: 풀, 독` 포함 | 필수 |
| UT-WEAK-06 | 없는 포켓몬 | `"없는포켓몬"` | `포켓몬을 찾을 수 없습니다.` | 필수 |
| UT-WEAK-07 | 1배 타입 생략 확인 | 임의 포켓몬 | 1배 타입은 출력에 포함되지 않음 | 선택 |
| UT-WEAK-08 | 부분 이름 매칭 | `"이상해"` | 이상해씨 결과 반환 | 선택 |

### 2.6 ChatHistory 모듈

| TC-ID | 테스트명 | 입력 | 기대 결과 | 우선순위 |
|-------|---------|------|---------|---------|
| UT-CH-01 | 테이블 생성 | `init_tables()` | 오류 없이 실행, 멱등성 확인 | 필수 |
| UT-CH-02 | 세션 생성 | `create_session("테스트", "gpt-4o-mini")` | 양수 session_id 반환 | 필수 |
| UT-CH-03 | 메시지 저장 | `save_message(1, "user", "안녕")` | 오류 없이 저장 | 필수 |
| UT-CH-04 | 메시지 조회 | `load_messages(1)` | 저장 순서대로 반환 | 필수 |
| UT-CH-05 | 세션 삭제 및 CASCADE | `delete_session(1)` | 세션 + 메시지 모두 삭제 | 필수 |
| UT-CH-06 | 세션 목록 조회 | `load_sessions(limit=60)` | 최대 60개 반환 | 선택 |
| UT-CH-07 | 제목 60자 제한 | 100자 제목으로 세션 생성 | 60자로 truncate | 필수 |

---

## 3. 통합 테스트

### 3.1 Agent 통합 테스트

| TC-ID | 시나리오 | 질문 | 기대 동작 | 기대 툴 |
|-------|---------|------|---------|---------|
| IT-AG-01 | 단순 스탯 조회 | "피카츄 HP는?" | SQL 조회 후 정확한 수치 답변 | `search_pokemon_db` |
| IT-AG-02 | 의미 기반 검색 | "바다 냄새 나는 포켓몬 알려줘" | 벡터 검색 후 관련 도감 설명 포함 답변 | `search_flavor_text` |
| IT-AG-03 | 진화 경로 | "피카츄 진화하면?" | Neo4j 탐색 후 라이츄 언급 | `search_evolution_chain` |
| IT-AG-04 | 타입 상성 (타입명 기반) | "불꽃 타입 약점은?" | 물, 바위, 땅 언급, 무효 타입 포함 | `search_type_relations` |
| IT-AG-05 | 복합 질문 | "공격력 높은 불꽃 타입 포켓몬 추천" | SQL + 벡터 복합 사용 | 2개 이상 |
| IT-AG-06 | SQL 자동 복구 | LLM이 잘못된 컬럼명 SQL 생성 유도 | 오류 후 수정 SQL로 재시도 성공 | `search_pokemon_db` ×2 |
| IT-AG-07 | 툴 호출 2회 제한 | 반복 정보 요청 질문 | 2회 이후 강제 답변 생성 | 최대 2회 |
| IT-AG-09 | 포켓몬 약점 조회 (포켓몬명 기반) | "리자몽 약점은?" | `search_pokemon_weakness` 선택, 4배 약점(바위) 포함 | `search_pokemon_weakness` |
| IT-AG-10 | 듀얼 타입 4배 정확도 | "이상해씨 약점 알려줘" | 독 + 풀 복합 배율 정확 반영 | `search_pokemon_weakness` |
| IT-AG-11 | 툴 라우팅 구분 | "땅 타입에 대해 알려줘" vs "이상해씨 약점은?" | 전자 → `search_type_relations`, 후자 → `search_pokemon_weakness` | 각각 별도 |

### 3.2 멀티턴 대화 통합 테스트

| TC-ID | 턴 | 질문 | 기대 결과 |
|-------|-----|------|---------|
| IT-MT-01 | 1 | "피카츄 스탯 알려줘" | 스탯 테이블 반환 |
| IT-MT-01 | 2 | "그럼 라이츄는?" | 이전 맥락(피카츄) 없이도 라이츄 스탯 반환 |
| IT-MT-01 | 3 | "둘 중 공격력이 높은 건?" | 두 포켓몬 비교 후 정답 반환 |

---

## 4. E2E 테스트 시나리오

| TC-ID | 시나리오명 | 단계 | 기대 결과 |
|-------|---------|------|---------|
| E2E-01 | 새 세션 채팅 | ① POST /chat (session_id 없음) → ② 답변 수신 | session_id 자동 생성, 답변 정확 |
| E2E-02 | 세션 재개 | ① POST /sessions → ② POST /chat (session_id 포함) | 이전 대화 문맥 유지 |
| E2E-03 | 세션 목록 조회 | ① 3개 세션 생성 → ② GET /sessions | 3개 세션 반환, 최신순 정렬 |
| E2E-04 | 세션 메시지 조회 | ① 대화 3턴 → ② GET /sessions/1/messages | 6개 메시지 순서대로 반환 |
| E2E-05 | 세션 삭제 | ① 세션 생성 → ② DELETE /sessions/1 → ③ 메시지 조회 | 404 반환 |
| E2E-06 | 툴 사용 이력 | POST /chat 후 GET /sessions/1/messages | `used_tools` 배열에 실제 툴명 포함 |

---

## 5. 보안 테스트

| TC-ID | 테스트명 | 입력 | 기대 결과 |
|-------|---------|------|---------|
| SEC-01 | SQL 인젝션 — DROP | `'; DROP TABLE pokemon; --` | `오류: SELECT 문만 허용` |
| SEC-02 | SQL 인젝션 — UPDATE | `'; UPDATE pokemon SET name='hack' --` | `오류: UPDATE 명령 허용되지 않음` |
| SEC-03 | SQL 인젝션 — INSERT | `'; INSERT INTO pokemon VALUES (9999, 'hack')` | `오류: INSERT 명령 허용되지 않음` |
| SEC-04 | 코드 블록 우회 시도 | ` ```sql DROP TABLE types ``` ` | 코드 블록 제거 후 DROP 차단 |

---

## 6. 성능 테스트

| TC-ID | 시나리오 | 측정 항목 | 목표 기준 |
|-------|---------|---------|---------|
| PERF-01 | 단순 SQL 조회 | 응답 시간 | ≤ 3초 |
| PERF-02 | 벡터 검색 포함 | 응답 시간 | ≤ 8초 |
| PERF-03 | 복합 툴(2회) 호출 | 응답 시간 | ≤ 15초 |
| PERF-04 | 임베딩 초기화 (전체) | 총 소요 시간 | ≤ 10분 |
| PERF-05 | 세션 목록 조회 (60개) | DB 응답 시간 | ≤ 0.5초 |

---

## 7. 테스트 환경 설정

```python
# conftest.py
import pytest
import psycopg2

TEST_DB_URL = "postgresql://postgres:postgres@localhost:5433/pokemon_test_db"

@pytest.fixture(scope="session")
def pg_conn():
    conn = psycopg2.connect(TEST_DB_URL)
    yield conn
    conn.close()

@pytest.fixture(autouse=True)
def rollback(pg_conn):
    """각 테스트 후 자동 롤백"""
    yield
    pg_conn.rollback()
```

---

## 8. 테스트 커버리지 목표

| 모듈 | 목표 커버리지 |
|------|------------|
| `pokemon_agent.py` (Tool 함수) | 90% 이상 |
| `chat_history.py` | 95% 이상 |
| `pokemon_neo4j.py` | 85% 이상 |
| `ingest.py` | 80% 이상 |
| 전체 평균 | 88% 이상 |
